<h1>Listing pages_articles</h1>

<table>
  <tr>
    <th>Page</th>
    <th>Article</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pages_articles.each do |pages_article| %>
  <tr>
    <td><%= pages_article.page_id %></td>
    <td><%= pages_article.article_id %></td>
    <td><%= link_to 'Show', pages_article %></td>
    <td><%= link_to 'Edit', edit_pages_article_path(pages_article) %></td>
    <td><%= link_to 'Destroy', pages_article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pages article', new_pages_article_path %>
